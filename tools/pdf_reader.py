def extract_pdf_text(path: str) -> str:
    """Placeholder PDF extractor for future real document ingestion."""
    return f"PDF extraction is not yet wired for {path}. Add PyMuPDF or pypdf integration in the next phase."
